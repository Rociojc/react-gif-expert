import React from 'react'
import ReactDOM from 'react-dom/client'
import GitExpectApp from './GitExpectApp'

import './styles.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GitExpectApp/>
  </React.StrictMode>
)
